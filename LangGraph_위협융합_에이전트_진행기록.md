# LangGraph 기반 다중 센서 위협 융합 및 상황보고 자동화 에이전트 — 진행 기록

(범용 개발 개념은 `개발_기초_개념_정리.md`를 참고하세요. 이 파일은 이 프로젝트 전용 기록입니다.)

## 진행 방식 메모

- **Phase가 끝날 때마다 코드 리뷰(퀴즈 형식)를 진행한다.** Claude가 바로 설명하지 않고, 사용자가 먼저 코드를 보고 본인 말로 설명하게 한 뒤 맞았는지 확인 + 놓친 부분 보완하는 방식. 실제 방산기업 1차 면접(기술 과제 발표 + 실무진 질의응답)을 대비하는 목적.
- 리뷰가 진행 중 끝나지 않으면 다음 세션 시작 전에 이어서 진행한다.
- **질문 처리 방식**: 진행 중 질문이 생기면 Claude가 판단해서, 지금 바로 답해야 이해에 도움되는 것(화면에 뜬 걸 보면서 이해해야 하는 개념 등)은 즉시 답변하고, 급하지 않은 건 메모해뒀다가 해당 Phase 끝날 때 한 번에 정리해서 답변한다.

### 코드 리뷰 완료 기록 (2026-08-06 세션)

Phase 1 확장분(IFF/geofence/audit log) 리뷰 전체 완료. 4개 파일 모두 검증함:

- [x] `fusion/identification.py` — FRIEND/HOSTILE 미판정 이유, NEUTRAL/UNKNOWN 분리 이유까지 이해 확인
- [x] `fusion/geofence.py` — `_bearing_deg`와 `diff` 계산의 역할 분리까지 이해 확인
- [x] `fusion/threat_scoring.py` — **기상 보정(x0.9)이 왜 1~5번 항목(레이더·열신호·SIGINT)에만 적용되고 7번(보호구역 거리·방향)에는 적용 안 되는지** 정확히 설명함: 1~5번은 센서의 "감지 능력"에 의존해 날씨 영향을 받지만, 보호구역 거리·방향은 순수 좌표 계산(수학)이라 날씨와 무관함. 코드의 계산 순서(기상 보정 → 보호구역 가산)가 의도한 설계임을 검증.
- [x] `agent/graph.py`의 `analyst_review` 노드 — **실제 설계 허점을 새로 발견함**: `interrupt()` 호출 후에 감사 로그 기록 코드(`audit_log.log_decision`)가 있어서, 분석관이 승인/거부 없이 프로그램을 종료해버리면 감사 로그가 전혀 남지 않는 문제를 확인. "판단을 회피한 사실 자체가 기록에 안 남는" 구조적 허점 — Phase 5(대시보드) 작업 시 수정 필요 (interrupt 호출 "전"에 대기 시작 로그를 먼저 남기는 방식으로 보완 예정).

### Phase 2 완료 — GitHub 업로드 및 트러블슈팅 기록 (2026-08-10 세션)

**결과**: `https://github.com/Deukseon/sitrep_fusion_agent` 에 전체 코드 + README(아키텍처 다이어그램·5단계 시나리오 인포그래픽 렌더링 확인) 업로드 완료.

**겪은 문제와 원인·해결 (실무에서도 흔히 겪는 Git 협업 문제들이라 기록 남김):**

| 문제 | 원인 | 해결 |
|---|---|---|
| `docs` 폴더가 `files` 상위 폴더에 저장됨 | 다운로드 시 브라우저가 엉뚱한 위치에 저장 | `mkdir docs` + `move`로 정리 |
| `ren docs\파일명 docs\새이름` 실행 시 에러 | `ren`(이름바꾸기)은 순수 이름만 받음, 경로 포함하면 "이동"으로 오인해 거부 | `cd docs`로 이동 후 파일명만 지정 |
| `git push` 시 `[rejected] fetch first` | GitHub 웹에서 이미지 파일을 직접 수정해서, 로컬과 원격이 서로 다른 커밋으로 갈라짐(diverge) | `git pull`로 원격 변경사항 먼저 받아오기 |
| `git pull` 시 "untracked files would be overwritten" | 로컬에 Git이 아직 추적 안 하는 동일 이름 파일이 있어서 충돌 위험 | 로컬 파일을 임시로 다른 이름으로 피신 후 pull |
| `git pull`이 띄운 편집기(Vim) 창을 그냥 닫아버림 | 병합 커밋 메시지 작성 화면인데 저장/종료 없이 닫아서 "병합 미완료" 상태로 남음 (`git status`에 "All conflicts fixed but you are still merging") | `git commit --no-edit`로 편집기 없이 기본 메시지로 병합 마무리 |

**핵심 교훈**: 로컬 작업과 GitHub 웹 직접 수정을 동시에 하면 반드시 `push` 거부가 발생함 — 한쪽만 골라서 작업하거나, 웹에서 수정했으면 로컬 작업 전에 항상 `git pull`부터 하는 습관이 필요.

## 1. 진행 현황

| 단계 | 상태 |
|---|---|
| B. 로컬 실행 | ✅ 완료 — 파이썬 설치, 폴더 구조 재정리, 패키지 설치, `main.py`/`visualize_map.py` 실제 API 실행까지 확인 |
| 동적 감시 + 무작위 테스트 | ✅ 완료 — `continuous_monitor.py`, `test_random_scenarios.py`, 429 대응 |
| **Phase 1 (작은 보완)** | ✅ 완료 — 아래 상세 참고 |
| A. GitHub 업로드 | ✅ 완료 — `github.com/Deukseon/sitrep_fusion_agent` 공개 저장소 |
| C. 배포 (FastAPI + 웹 서버) | 웹 주소로 접속 가능한 실제 서비스로 만들기 |
| 궤적(경로) 예측 | 상태 지속성 구조 설계 필요 (별도 진행 예정) |

### Phase 1 완료 상세

- **OpenSky 무료 계정 등록**: 완료. 등록 사용자 전환으로 하루 400 → 4000 크레딧 확보.
- **`.env` 자동 로드**: `config.py`에서 `load_dotenv()`를 한 번만 호출하도록 통합. `.env.example` 견본 파일 추가.
- **`config.py` 신설**: `MONITOR_BBOX`, `CENTER_LAT/LON`, `ANALYST_REVIEW_THRESHOLD`, `POLL_INTERVAL_SECONDS`를 여러 파일에서 한 곳으로 통합. 덤으로 `flight_tracker.py`에 OpenSky 등록 계정 OAuth2 인증(Bearer 토큰 발급·캐싱) 추가 — `.env`에 Client ID/Secret이 있으면 자동으로 인증 요청 사용.
- **`logging` 전환**: 라이브러리 모듈(`flight_tracker.py`, `agent/graph.py` 등)은 `logger`만 사용하고, 실행 진입점(`main.py`, `continuous_monitor.py`, `visualize_map.py`)에서 `logging.basicConfig`로 타임스탬프 포함 포맷을 설정하는 구조로 정리.
- **부수 개선**: 코드에 있던 구버전 모델명(`claude-sonnet-4-6`)을 최신 `claude-sonnet-5`로 업데이트.
- **검증**: `main.py`, `continuous_monitor.py`, `visualize_map.py`, `test_random_scenarios.py`, `test_interrupt_flow.py` 전부 목데이터로 재검증 완료.

### 실전 C4I 확장 검토 및 적용 (Phase 1 이후, GitHub 업로드 전)

스코어링 로직("어떻게 점수를 매기나")을 설명하다가, 더 실전 C4I스러운 방향이 있는지 논의함. 5개 후보를 검토하고 우선순위를 판단:

| 항목 | 판단 | 처리 |
|---|---|---|
| 1. IFF 피아식별 | 독립적, 바로 적용 가능 | ✅ 적용 (`fusion/identification.py`) |
| 2. 보호구역(geofence) 기반 판단 | 독립적, 바로 적용 가능 | ✅ 적용 (`fusion/geofence.py`) |
| 3. ROE(교전규칙) 제약 | COA(절차 선택지 제시) 기능이 먼저 있어야 의미 있음 | ⏸ 보류 — Phase 5(FastAPI)에서 COA와 함께 |
| 4. 의사결정 감사 로그 | 독립적, 바로 적용 가능 | ✅ 적용 (`audit_log.py`) |
| 5. 신뢰도 가중 센서 융합 | 여러 시점 데이터가 있어야 의미 있음 (지금은 매번 새 조회) | ⏸ 보류 — Phase 3(상태 지속성)와 통합 |

**적용 내용:**
- **IFF 피아식별** (`fusion/identification.py`): 위협점수와 별개의 축으로 `identity` 필드 추가. 실제 IFF 질의응답 데이터가 없으므로 `FRIEND`/`HOSTILE`은 판정하지 않고, 호출부호 유무로 `NEUTRAL`/`UNKNOWN`만 판정 (한계를 코드 주석에 명시).
- **보호구역 기반 판단** (`fusion/geofence.py`, `config.py`의 `PROTECTED_ZONES`): haversine 거리 + 방위각 비교로 "보호구역 반경 내" 여부와 "그 방향으로 접근 중"인지 판정. 반경 내 +15점, 접근 중 +10점을 위협 스코어링에 반영. 예시 좌표로 인천국제공항·부산김해 관제권 사용.
- **의사결정 감사 로그** (`audit_log.py`): 분석관의 승인/거부 결정을 `audit_log.jsonl`에 JSON Lines 형식으로 기록 (타임스탬프, 트랙 정보, 결정, 경보발령여부 포함). 나중에 pandas 등으로 바로 분석 가능한 형태.
- **기술 아키텍처 다이어그램** (`docs/architecture.svg`): 외부 API → 데이터 수집·센서 융합 → LangGraph 에이전트(자동 평가 + 분석관 승인) → 출력의 4계층 구조를 시각화. README 상단에 반영.
- **운영 시나리오 스토리보드** (`docs/scenario_storyboard.png`): 평시 감시 → 위협 평가/승인 대기 → 승인/경보 발령 → SITREP 생성·상급 보고, 4단계로 압축한 이미지. 전체 8단계 버전과 이미지 생성 프롬프트는 `운영_시나리오_스토리보드.md`에 별도 보관. README에 반영.
- 브리핑 텍스트와 지도 팝업에도 식별·보호구역 정보 반영.
- **미래 목표 관점 정리**: 지금까지의 최종 산출물은 "상황보고(SITREP) + 경보 발령 여부의 이분법적 승인"까지였음. COA(절차 선택지 제시, 예: 관찰만/육안확인요청/상급보고/즉시경보 중 선택)는 아직 없는 기능이며, Phase 5에서 ROE 필터링과 함께 다룰 예정.

**실제로 진행한 로컬 환경 구성** (참고용 요약 — 가상환경(venv)은 안 쓰고 시스템 파이썬에 바로 설치):
```powershell
# 1. python.org에서 파이썬 설치 (Add python.exe to PATH 체크 필수)
# 2. 프로젝트 폴더에서 PowerShell 실행
pip install -r requirements.txt
python main.py
```

---

## 2. 프로젝트 실행 스크립트 종류 정리

| 파일 | API 호출 여부 | 용도 |
|---|---|---|
| `main.py` | 실제 API 사용 | 1회 실행, HIGH 이상이면 콘솔에서 직접 approve/reject 입력 |
| `visualize_map.py` | 실제 API 사용 | 1회 실행 + 결과를 `threat_map.html` 지도로 저장 |
| `continuous_monitor.py` | 실제 API 사용 | 5분 간격으로 반복 감시, 승인 대기 없이 고위협 발견 시 로그만 남김 (Ctrl+C로 중단) |
| `test_interrupt_flow.py` | API 미사용(목데이터) | 분석관 승인/거부(interrupt) 흐름만 검증 |
| `test_random_scenarios.py` | API 미사용(무작위 생성) | 위협 스코어링 로직과 지도 색상 분포(빨강/주황/노랑/초록)를 API 크레딧 소모 없이 빠르게 확인 |

---

## 3. 프로젝트에서 쓰는 실제 외부 API 2가지

### OpenSky Network — 실시간 항공기 위치 API

- **무엇인가**: 전 세계 항공기의 실시간 위치(ADS-B 신호)를 수집해서 공개하는 비영리 네트워크. 스위스의 한 대학 연구 프로젝트로 시작됨.
- **데이터 출처**: 전 세계 자원봉사자들이 집 옥상 등에 소형 안테나를 달아 근처 항공기의 ADS-B 신호를 수신하고 OpenSky 서버로 모아주는 크라우드소싱 방식.
- **제공 정보**: 항공기 식별번호, 위경도, 고도, 속도, 방위각 — `flight_tracker.py`에서 "레이더 트랙"처럼 다루는 데이터.
- **왜 씀**: 실제 군사 레이더 데이터는 공개되지 않으므로, "실제로 하늘에 있는 진짜 물체"를 트랙 소스로 삼기 위해 사용.
- **인증**: 무료, API 키 불필요 (익명 사용 시 하루 400 크레딧 제한 있음 — 아래 상세)

### Open-Meteo — 실시간 기상 API

- **무엇인가**: 여러 기상청·기상 모델 데이터를 통합해서 무료로 제공하는 오픈소스 날씨 API.
- **데이터 출처**: 미국 NOAA, 독일 DWD, 유럽 ECMWF 등 각국 기상청의 공개 예보 모델 데이터를 재가공.
- **제공 정보**: 현재 기온, 풍속, 풍향, 날씨 코드(맑음/비/안개 등) — `weather_api.py`에서 "시정(가시거리) 판단"에 사용.
- **왜 씀**: 안개·강수 등 기상 상황이 센서 신뢰도에 영향을 준다는 걸 시뮬레이션하기 위해, 위협 점수 보정용으로 사용.
- **인증**: 무료, API 키 불필요

**공통점**: 회원가입/API 키 없이 쓸 수 있는 완전 공개 API라 데모·포트폴리오용으로 적합. 실제 상용 서비스는 보통 API 키 발급 + 훨씬 까다로운 요청 제한이 있는 유료 API를 씀.

### OpenSky 요청 제한(rate limit) — 정확한 수치

- 익명 사용자: **하루 400 크레딧**
- 등록 사용자(무료 가입): **하루 4000 크레딧**
- ADS-B 안테나를 실제로 기여하는 활성 사용자: **하루 8000 크레딧**
- `/states/all` 호출은 요청한 지역(bbox)의 **지리적 면적에 비례**해서 크레딧이 소모됨 — 한반도 전체처럼 넓은 구역을 조회하면 단순 "1회=1크레딧"이 아닐 수 있음

**한도 도달 위험 판단**: 하루에 `main.py`/`visualize_map.py`를 여러 차례 실행 + 동적 감시(폴링 루프)까지 더하면 익명 400 크레딧으로는 하루도 못 버틸 가능성이 있음. → **무료 계정 등록으로 4000 크레딧 확보**가 가장 효과적인 대응 (아직 미실행 — 스케줄 참고).

**429(요청 초과) 대응 완료**: `flight_tracker.py`에 429 응답을 명시적으로 감지하는 코드를 추가함 — 한도 초과 시 일반 에러가 아니라 "요청 한도 초과, N초 후 재시도 권장"이라고 명확히 알려주고, 프로그램은 죽지 않고 빈 리스트로 안전하게 넘어감.

---

## 4. 설치 패키지 요약

| 패키지 | 역할 |
|---|---|
| requests | 웹 API 호출용 기본 도구. flight_tracker.py, weather_api.py에서 사용 |
| langgraph | 노드/엣지로 파이프라인을 구성하는 핵심 프레임워크 |
| langchain-core | LangGraph가 내부적으로 쓰는 기반 라이브러리 |
| langchain-anthropic | Claude API를 LangChain 방식으로 호출하는 연결 모듈. generate_brief 노드에서 사용 |
| python-dotenv | .env 파일의 API 키를 코드에서 읽어오게 해주는 도구 (설치는 됐으나 코드에서 아직 실제로 안 씀 — 보완 필요) |
| pydantic | 데이터 구조 타입 검증 라이브러리. LangGraph 내부 상태 검증에 사용 |
| folium | 위경도 데이터를 인터랙티브 HTML 지도로 그려주는 라이브러리. `visualize_map.py`에서 사용 |

---

## 5. 프로젝트 발전 방향 — 스냅샷에서 동적 감시로 (구현 완료)

### "완전 자동화(승인까지 무인)"의 제약 — 판단 결과

계속 이어지는 감시 + 사람 승인까지 전부 무인으로 만들려 했더니 제약이 있었습니다:

| 제약 | 내용 |
|---|---|
| 컴퓨터/터미널 상시 구동 | 프로그램이 백그라운드로 계속 돌아야 하고, 꺼버리면 감시 중단 |
| API 크레딧 소모 | 짧은 간격으로 폴링하면 하루 400 크레딧(익명) 금방 소진 |
| 분석관 승인이 터미널을 막음 | `input()` 방식은 사람이 그 순간 안 붙어있으면 루프 전체가 멈춰버림 |

**채택한 절충안**: `continuous_monitor.py`는 5분 간격으로 실제 폴링하되, 승인 대기 없이 **HIGH 이상 발견 시 로그만 남기고 계속 진행**하도록 구현. "진짜 승인 버튼" 워크플로우는 웹 서비스(C단계, FastAPI)로 갈 때 다시 다루는 게 자연스러운 자리 — 웹은 브라우저에서 언제든 버튼을 누를 수 있지만 터미널은 그 순간 사람이 붙어있어야 하기 때문.

### 무작위 테스트 데이터 (API 크레딧 절약)

`test_random_scenarios.py`로 실제 API 호출 없이 위협 스코어링/지도 색상 분포를 검증 가능. 저고도·고속·미확인 조합이 35% 확률로 섞이도록 설계해서, 실행할 때마다 CRITICAL/HIGH(빨강/주황)가 자연스럽게 섞여 나옴.

### 실제 방산업체의 감시 체계 — 참고 (일반적 패턴, Hanwha 내부 확인 정보 아님)

- OpenSky 같은 민간 공개 API는 군사 작전에 쓰지 않음 (신뢰성·보안·커버리지 문제 — 적기는 ADS-B 신호를 보내지 않음)
- 실제로는 자체 레이더·IFF·EO/IR 센서를 직접 통합하고, Link-16·KVMF 같은 군용 전용 데이터링크로 부대 간 데이터 교환
- "API 구독료" 개념이 아니라 레이더 장비 자체가 고가 자산이고, 통합 시스템은 방위사업 정부 계약으로 조달
- 이 프로젝트처럼 "공개 API + 합성 데이터로 대체"하는 접근은 실제 데이터 접근이 불가능한 상황에서 파이프라인 설계 역량을 보여주는 정직한 방법 — 면접에서 그대로 설명 가능

---

## 6. 프로젝트 전반 점검 (포트폴리오 관점 보완 포인트)

| 영역 | 현재 상태 | 보완 방향 |
|---|---|---|
| 상태 지속성 | 매 실행마다 처음부터 재조회, 이전 항적 기억 못 함 | 궤적 추적의 전제조건 — 가장 우선순위 높음 (Phase 3에서 진행) |
| API 키 관리 | ✅ 완료 — config.py에서 .env 자동 로드 | |
| 설정값 하드코딩 | ✅ 완료 — config.py로 분리 | |
| API 한도 대응 | ✅ 완료 — 429 감지 + OpenSky 등록 계정 인증(4000 크레딧) | |
| 로깅 | ✅ 완료 — logging 모듈로 전환 | |
| 테스트 커버리지 | interrupt 흐름만 테스트됨 | threat_scoring.py의 경계값(55점 등) 테스트 추가 필요 |

---

## 7. Phase 3 착수 — 폴링 간격 vs 고속 위협 탐지 검증 (2026-08-10 세션)

### 설계 방향 재확인

"전투기를 고려 안 하고 설계한 게 아닌가"라는 의문 제기 → 검토 결과, 시스템은 **애초부터 위협(전투기급) 중심으로 설계됨** (threat_scoring.py의 스텔스성·SIGINT 요소, identification.py의 IFF 개념 자체가 민간 관제가 아닌 군사 위협평가 프레임). 두 가지 방향 중 판단:
- **1번(한계 인정 + 원래 계획 유지) 채택**, 2번(갈아엎기) 기각
- 기각 이유: 실제 전투기는 ADS-B를 꺼서 어떤 공개 API로 바꿔도 못 잡음 — "갈아엎어도 결국 지금처럼 합성 데이터로 흉내내는 구조로 돌아올 수밖에 없음"

### 새로 만든 검증 도구

- **`synthetic_flight_path.py`**: 시간에 따라 실제로 이동하는 가상 항적 시뮬레이터. 실제 API 데이터는 "진짜 위치"를 모르기 때문에 탐지 성공/실패를 검증할 기준(ground truth)이 없음 — 이 시뮬레이터가 그 기준을 제공. 프로덕션 파이프라인에 연결되지 않는 순수 검증 도구.
- **`test_polling_detection.py`**: 속도(여객기~초음속 600m/s) × 경로(중앙관통/중간스침/가장자리스침) × 폴링간격(10~300초) 조합별 탐지 성공 여부를 수치로 검증.
- `fusion/geofence.py`에 `distance_km()` 공개 함수 추가 (기존 `_haversine_km`를 다른 모듈에서 재사용 가능하도록).

### 핵심 발견

| 조건 | 결과 |
|---|---|
| 중앙 관통 경로 | 400m/s 이상만 300초 간격에서 놓침, 120초부터는 전부 탐지 |
| **가장자리 스침 경로(반경의 90% 지점)** | **250m/s부터 300초 간격에서 놓침, 400m/s 이상은 120초·60초까지도 놓침. 30초부터 안정적으로 탐지** |
| 상세 예시 (가장자리 스침, 600m/s, 300초 간격) | 실제로는 구역(반경 20km)에 18km까지 진입(`true_entered_zone=True`)했지만, 폴링 시점이 안 맞아 시스템이 전혀 탐지 못 함 |

**결론**: "몇 초부터 확실히 놓치는가"가 아니라 "가장자리를 스치는 고속 위협은 매우 짧은 간격(30초 이하)이 아니면 원천적으로 놓칠 수 있다"는 게 핵심.

### API 크레딧 계산 (OpenSky 공식 문서 확인)

`/states/all`은 바운딩박스 면적(제곱도) 기준 등급제: 25제곱도 이하 1크레딧, 25~100제곱도 2크레딧, 100~400제곱도 3크레딧, 400제곱도 초과 4크레딧. 등록 사용자는 하루 4000크레딧(엔드포인트별 독립).

```
현재 MONITOR_BBOX(한반도 근해) = 48제곱도 → 2크레딧/회 → 최대 하루 2000회 → 최소 지속가능 간격 43.2초
   → 목표(30초 이하)에 미달

개선안: 감시구역을 보호구역 주변으로 좁혀 25제곱도 이하로 만들면
   → 1크레딧/회 → 최대 하루 4000회 → 최소 지속가능 간격 21.6초
   → 목표(30초) 여유 있게 충족
```

### 다음 실행 과제 (2026-08-11 세션에서 모두 반영 완료)

- [x] `config.py`의 `MONITOR_BBOX`를 25제곱도 이하로 좁히기 → 14제곱도(인천·부산김해 관제권 포함, 여유 0.5도 이상)로 축소, 1크레딧/회로 전환
- [x] `POLL_INTERVAL_SECONDS`를 30초 근방으로 재조정 → 300초 → 30초
- [ ] README/진행기록에 "전투기가 ADS-B를 끄면 폴링 간격과 무관하게 원천적으로 탐지 불가" 한계 명시 — 아직 미반영, 다음 세션으로 이월
- [x] `TrackHistory`(과거 관측 이력 저장 구조) 설계 완료 (아래 섹션 참고)

---

## 8. Phase 3 완료 — TrackHistory 구현 (2026-08-11 세션)

### 설계 개요

기존 `continuous_monitor.py`는 매 사이클마다 새 `thread_id`로 시작해서, 같은 항공기(`icao24`)를 여러 폴링에 걸쳐 추적하지 못했다. `fusion/track_history.py`를 신규 작성해서 이 문제를 해결:

- **저장 방식**: 파일 기반 JSON (`track_history.json`, 프로젝트 루트, `.gitignore`에 추가함 — 실행할 때마다 쌓이는 산출물이라 커밋 대상 아님). 사용자가 "재시작해도 이력이 유지돼야 한다"고 판단해서 메모리 방식 대신 파일 방식 선택.
- **핵심 함수**:
  - `update_history(history, tracks, timestamp)` — 이번 사이클 관측치를 트랙별로 누적, 트랙당 최근 20개 초과분은 자동 삭제
  - `compute_velocity_from_history(points)` — 최근 두 관측점의 실제 이동거리(`fusion/geofence.py`의 `distance_km`) ÷ 시간으로 속도를, 두 점을 잇는 방위각(`bearing_deg`, 이번에 `geofence.py`에서 공개 함수로 뺌)으로 방향을 계산. 관측이 1번뿐이면(첫 관측) `None` 반환 → 이 경우 API 원본값으로 폴백
  - `_prune_stale_tracks` — 1시간(`STALE_SECONDS`) 이상 재관측 안 된 트랙은 자동 정리, 파일이 무한정 커지지 않게 함
- **`continuous_monitor.py` 개조**: `main()`에서 이력을 한 번 로드 → 사이클마다 `run_one_cycle(cycle_num, history)`로 이력을 넘기고 갱신된 이력을 반환받아 저장. `Ctrl+C`로 종료해도 저장하도록 처리.

### 검증 (`test_track_history.py`, 목데이터, 4개 항목 전부 통과)

1. 같은 트랙이 3사이클에 걸쳐 이력에 누적되는가
2. 2회 이상 관측 시 계산된 속도(29.4m/s)·방향(90도, 정동)이 실제 이동 패턴과 일치하는가
3. 파일로 저장 후 다시 로드해도 이력이 유지되는가 (영속성)
4. `STALE_SECONDS` 초과 트랙만 정확히 정리되는가

### 아직 안 한 것 (의도적으로 남겨둔 범위)

`fusion/geofence.py`의 "보호구역 방향으로 접근 중인가" 판정은 여전히 API가 준 순간 `heading_deg`만 사용한다. 이번에 계산한 `computed_heading_deg`를 스코어링에 연결하면 범위가 너무 커질 것 같아 일부러 남겨뒀다 — **Phase 4(경로 예측)에서 자연스럽게 이어붙이는 걸 권장.**

### 사고 확장 — "계산속도 250~300m/s면 전투기 아닌가?" (2026-08-11)

실제 실행 로그에서 이력 기반 계산속도가 250~300m/s로 나오는 트랙을 보고, "이 정도면 여객기가 아니라 전투기 아닌가?"라는 질문이 나왔다. 검토 결과 **결론은 "아니다"** — 속도값 단독으로는 여객기와 전투기를 구분할 수 없다는 게 핵심.

**근거 1 — 여객기도 원래 이 속도대에서 난다.** 일반 여객기 순항 지상속도는 900km/h(≈250m/s) 안팎이 기본이고, 순항고도에서 제트기류(순풍)를 타면 지상속도가 1200km/h(≈333m/s)를 넘기는 경우도 드물지 않다. 이건 실제로 음속을 돌파한 게 아니라 바람을 업고 날아서 "지상에서 관측되는 속도"만 빨라지는 현상이다 (신문 기사에도 종종 나오는 "여객기가 초음속으로 날았다"는 표현이 정확히 이 얘기).

**근거 2 — 전투기 순항속도 자체가 이 구간과 겹친다.** 전투기 최대속도는 마하 1.5~2.5(약 515~860m/s)까지 나오지만, 그건 애프터버너를 켜고 전력 질주할 때 얘기고 **평상시 순항은 여객기와 비슷하게 아음속(마하 0.8~0.9, 약 270~300m/s)**으로 다닌다. 즉 속도값만으로는 두 기종의 순항 상태를 구분할 방법이 원리적으로 없다.

**결론 및 프로젝트 설계와의 연결**: `fusion/threat_scoring.py`가 애초에 이 문제를 알고 설계돼 있었다 — 속도 단독이 아니라 **"저고도(1500m 미만) + 고속(100m/s 초과)" 조합**만 위협 신호로 잡는다. 여객기 순항고도는 보통 9,000~12,000m라 이 규칙에 아예 안 걸린다. 그래서 판단 기준은 "속도가 얼마냐"가 아니라 **"그 속도가 어느 고도에서 나왔냐"**로 옮겨가야 한다.

**실행한 개선**: `continuous_monitor.py`의 이력 기반 재계산 로그에 고도(`altitude_m`)를 함께 출력하도록 수정. 이제 실행 로그만 보고도 "고고도(9km+) + 250~300m/s = 여객기", "저고도(1500m 미만) + 250~300m/s = 진짜 흥미로운 케이스"를 바로 구분할 수 있다.

**후속 아이디어(당장 반영 안 함)**: 호출부호(`label`) 유무나 항로 일관성까지 같이 보면 더 정확한 판별이 가능하다. 이건 `fusion/identification.py`의 식별(identity) 축과 연결 지점이 있어 보이나, 지금은 로그 확인 수준에서 마무리.

---

## 다음에 해보면 좋을 것

- [x] API 키 개념 이해
- [x] 배포 vs 업로드 개념 이해
- [x] PowerShell로 프로젝트 폴더 구조 재정리
- [x] 파이썬 설치 (python.org, PATH 등록)
- [x] `pip install -r requirements.txt` 실행
- [x] `python main.py` 실행 — 실제 OpenSky/Open-Meteo API 응답 확인 (103건 수집 성공)
- [x] `python visualize_map.py` 실행 — 지도 시각화 + None 결측치 버그 수정
- [x] 동적 감시(`continuous_monitor.py`) + 429 대응 + 무작위 테스트(`test_random_scenarios.py`) 구현
- [x] OpenSky 무료 계정 등록 (400 → 4000 크레딧)
- [x] Phase 1: config.py 분리, .env 자동 로드, logging 전환
- [x] `.env`에 OpenSky Secret 값 입력 후 실제 실행으로 4000 크레딧 인증 확인
- [x] A. `.gitignore` 작성 + GitHub 업로드 실습 (Git 협업 트러블슈팅 포함)
- [x] Phase 3: TrackHistory 구현, 감시구역 축소(48→14제곱도), 폴링간격 재조정(300→30초)
- [ ] C. FastAPI로 감싸서 배포하기 (관제 대시보드/COP)
- [ ] 궤적(경로) 예측 — TrackHistory 완료로 이제 착수 가능 (Phase 4)
- [ ] (선택) LICENSE 추가 — 프로젝트 완성 후 몰아서 진행 예정

세부 일정은 `프로젝트_진행_스케줄.md` 참고.
