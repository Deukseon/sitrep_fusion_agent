# 개발 기초 개념 정리

이 프로젝트뿐 아니라 앞으로 다른 개발 작업에도 두고두고 참고할 수 있는 범용 개념들을 모았습니다.
(프로젝트 전용 진행 기록은 `LangGraph_위협융합_에이전트_진행기록.md`를 참고하세요.)

## 1. 연구 워크플로우 vs 개발 워크플로우

| | 연구 워크플로우 | 개발 워크플로우 |
|---|---|---|
| 목표 | 결과/신뢰도 수치 도출 | 남이 실행 가능한 프로그램 |
| 실행 환경 | 내 컴퓨터, 내가 알면 됨 | 남의 컴퓨터에서도 동일하게 돌아가야 함 |
| 기록 방식 | 논문에 방법론 서술 | README + 코드 자체가 기록 |
| 버전 관리 | 파일명에 v1, v2, final... | Git으로 변경 이력 추적 |
| 검증 방식 | 결과 수치/그래프로 확인 | 테스트 코드로 자동 검증 |

**한 줄 요약**: 연구는 "내가 결과를 냈다"가 목표, 개발은 "누구나 재현 가능하다"가 목표.

---

## 2. GitHub 프로젝트에 흔히 있는 파일/폴더 틀

```
프로젝트-이름/
├── README.md          ← 필수. 프로젝트 설명서
├── requirements.txt    ← 필수급. "이거 설치하면 돌아감" 패키지 목록
├── .gitignore          ← 필수급. Git에 올리면 안 되는 파일 제외 목록
├── LICENSE             ← 선택. 남이 이 코드를 써도 되는지 명시
├── src/ (또는 도메인별 코드 폴더들)
└── tests/              ← 있으면 좋음. 검증(테스트) 코드
```

### README.md
- "read me(먼저 읽어주세요)"라는 뜻 그대로, 프로젝트 안내문
- 보통 들어가는 내용: 프로젝트 목적 / 파일·폴더 구조 / 설치·실행 방법 / 설계 이유
- GitHub에 프로젝트를 올리면 첫 화면에 자동으로 표시됨 → 면접관이 코드보다 먼저 읽는 문서

### requirements.txt
- 이 프로젝트를 돌리는 데 필요한 파이썬 패키지와 버전을 적어둔 목록
- `pip install -r requirements.txt` 한 줄로 필요한 걸 전부 설치 가능하게 해줌

### .gitignore
- 연구할 때는 데이터 파일도 그냥 폴더에 두고 썼겠지만, 개발에서는 아래 항목들을 GitHub에 실수로 올리지 않도록 미리 제외 목록을 만들어둠:
  - `__pycache__/` 같은 자동 생성 캐시 파일
  - API 키, 비밀번호 등 민감정보
  - 용량 큰 데이터 파일

### tests/ (테스트 코드)
- 코드가 의도대로 동작하는지 자동으로 확인하는 스크립트
- 예: `test_interrupt_flow.py` — 실제 API 없이 목데이터로 "승인/거부 흐름이 제대로 동작하는가"를 자동 검증

---

## 3. Git/GitHub 기본 흐름

```bash
git init                              # 이 폴더에서 버전관리 시작
git add .                             # 변경사항을 커밋 대상으로 담기
git commit -m "위협 스코어링 로직 추가"    # 스냅샷(버전) 저장
git push                              # GitHub 저장소에 업로드
```

**연구에 빗대면**: `git commit`은 "이 결과 버전을 기록해두는 것"에 가깝습니다. 나중에 "3번째 버전에서 뭘 바꿨더라?"를 되짚어볼 수 있고, 여러 버전을 파일명(v1, v2, final, final2...)으로 구분하지 않아도 됩니다.

---

## 4. API 키란 정확히 무엇인가

**API 키는 "이 서비스를 쓸 자격이 있다"는 걸 증명하는 비밀번호 같은 문자열**입니다.

예를 들어 코드에서:
```python
llm = ChatAnthropic(model="claude-sonnet-4-6", max_tokens=500)
```
이 코드가 실제로 동작하려면 `ANTHROPIC_API_KEY`라는 값이 필요합니다. Anthropic 서버에 "저 이런 요청 보낼게요"라고 할 때, 이 키를 같이 보내야 서버가 "너 누구야? 결제할 계정 있어?"를 확인할 수 있습니다.

| | 용도 |
|---|---|
| 아이디/비밀번호 | 사람이 웹사이트에 로그인할 때 씀 |
| API 키 | 프로그램이 다른 서버에 요청 보낼 때 씀 (코드가 사람 대신 "로그인"하는 셈) |

그래서 `.env` 파일에 넣고 GitHub엔 절대 안 올립니다. 키가 유출되면 남이 내 계정으로 비용을 쓸 수 있어서, 신용카드 번호 유출과 비슷한 위험도를 가집니다.

---

## 5. "서비스 배포"는 GitHub 업로드와 다른 개념

- **GitHub 업로드** = 코드를 저장/공유하는 것 (도서관에 책을 꽂아두는 것과 비슷 — 누구나 "읽을" 순 있지만 "실행 중"은 아님)
- **배포(deploy)** = 그 코드를 서버에서 실제로 계속 돌아가게 만드는 것 (책 내용대로 공장을 실제로 가동시키는 것)

예를 들어 `main.py`는:
- GitHub에 올리면 → 남이 코드를 보고 다운받을 수 있음 (배포 아님)
- 어딘가의 서버(AWS, 클라우드 등)에 올려서 24시간 실행되게 하고, 웹 주소로 접속 가능하게 만들면 → 그게 배포

---

## 6. PowerShell(커맨드라인)로 폴더 정리해본 것 — CLI가 뭔지에 대한 감

파일이 폴더 구조 없이 평평하게 저장돼서, PowerShell 명령어로 하위 폴더를 만들고 파일을 이동시켰습니다:

```powershell
mkdir data_sources, fusion, agent
move flight_tracker.py, synthetic_sensors.py, weather_api.py data_sources
move threat_scoring.py fusion
move graph.py agent
```

**평소 마우스로 하던 일(폴더 새로 만들기, 파일 드래그해서 옮기기)이 사실 전부 텍스트 명령으로도 가능하다**는 걸 직접 체감하신 부분이 핵심입니다.

- 마우스 클릭 = GUI(그래픽 사용자 인터페이스)로 컴퓨터에 명령
- 커맨드라인(PowerShell, 터미널) = 텍스트로 컴퓨터에 명령

내부적으로는 결국 같은 일(파일 시스템에 "이 파일을 이 경로로 옮겨라"라는 명령을 운영체제에 전달)을 하는 건데, GUI는 이걸 아이콘·드래그로 감싸놓은 것이고 CLI는 그 명령을 직접 타이핑하는 것입니다. 개발자들이 CLI를 선호하는 이유도 여기 있어요 — **같은 작업을 스크립트로 만들어두면, 나중에 몇백 개 파일이라도 명령어 한 줄로 똑같이 반복** 시킬 수 있기 때문입니다 (마우스로는 하나하나 드래그해야 하죠).

---

## 7. None vs NaN vs null vs N/A — 헷갈리는 "값 없음" 4형제

### N/A 이름을 뜯어보면

**N/A = "N"ot "A"vailable** ("이용 불가능")를 줄인 표기입니다.

- N = **N**ot
- A = **A**vailable

즉 "값이 없다"가 아니라 정확히는 **"(어떤 이유로든) 이용할 수 없는 값"**이라는 뜻입니다. 엑셀, 설문지, 웹사이트 등 사람이 읽는 화면 어디서나 이 약자를 관용적으로 씁니다. `_fmt()` 함수가 `None`을 화면에 보여줄 때 `"N/A"`로 바꿔주는 것도, "이 항공기는 고도 값을 이용할 수 없다"는 뜻을 사람이 읽기 편하게 표현한 것입니다.

### 넷을 한 번에 비교

| | 뜻 | 쓰이는 곳 | 정체 |
|---|---|---|---|
| **None** | "이 변수엔 아무 값도 할당 안 됨" | 파이썬 전반 (문자열, 리스트, 딕셔너리 등 모든 곳) | 파이썬 언어의 `NoneType` 값 |
| **null** | None과 동일 개념, 다른 언어에서의 이름 | JavaScript, Java, C# 등 | 해당 언어의 특수 타입 |
| **NaN** | "이건 숫자 계산이 불가능/정의 안 됨" (Not a Number) | 주로 숫자 배열(pandas, numpy) 안에서 | `float`의 특수값 |
| **N/A** | "이용 불가능한 값" (Not Available) | 화면 출력, 문서, 엑셀 — 사람이 읽는 곳 | 그냥 문자열(텍스트) |

**핵심 구분**: `None`·`NaN`·`null`은 **컴퓨터가 내부적으로 다루는 값**이고, `N/A`는 **사람이 보는 화면용 텍스트**입니다. 그래서 코드에서 `if value is None`으로 판단(컴퓨터용)한 다음, `"N/A"`라는 문자열을 반환(사람용 표시)하는 흐름이 자연스러운 겁니다.
