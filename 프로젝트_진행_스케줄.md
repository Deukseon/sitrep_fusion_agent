# 프로젝트 진행 스케줄

LangGraph 기반 다중 센서 위협 융합 및 상황보고 자동화 에이전트 — 앞으로 남은 작업을 순서·난이도·의존관계 기준으로 정리했습니다.

**정렬 기준**: 뒤 단계가 앞 단계에 의존하는 경우 순서를 강제했고, 의존관계가 없는 건 "쉬운 것 먼저 → 포트폴리오 임팩트 큰 것"순으로 배치했습니다.

---

## Phase 0 — 지금까지 완료 (참고용)

- [x] 파이프라인 설계 (수집→융합→스코어링→분석관 확인→브리핑)
- [x] 실제 API 연동 (OpenSky, Open-Meteo) 로컬 실행 검증
- [x] 지도 시각화 (folium)
- [x] 동적 감시 루프 + 429 대응 + 무작위 테스트 데이터

---

## Phase 1 — 작은 보완 (선행 정리, 난이도 하) ✅ 완료

GitHub에 올리기 전에 정리해두면 좋은 것들입니다. 순서는 자유롭게 섞어도 무방합니다.

| 작업 | 왜 지금 하는가 | 예상 난이도 | 상태 |
|---|---|---|---|
| OpenSky 무료 계정 등록 | 400→4000 크레딧, Phase 3(동적 감시 본격 사용) 전에 필요 | 하 | ✅ 완료 |
| `.env` 자동 로드 적용 | 지금은 매번 `export`로 수동 입력 중. `python-dotenv`가 이미 설치돼 있으니 코드 3줄 추가 | 하 | ✅ 완료 |
| `config.py`(또는 `.yaml`)로 설정값 분리 | `MONITOR_BBOX`, 임계값(55점) 등 하드코딩된 값 정리 — GitHub에 올릴 때 "설정 가능한 시스템"으로 보이게 함 | 하 | ✅ 완료 |
| `logging` 모듈로 전환 | `print()` → `logging`으로 바꾸면 실제 운영 시스템처럼 보임 (README에서도 어필 포인트) | 하 | ✅ 완료 |

**이 Phase의 목적**: GitHub에 올렸을 때 "학습용 스크립트"가 아니라 "설정 가능한 시스템"으로 보이게 하는 마감 작업.

---

## Phase 2 — A. GitHub 업로드 (다음 단계)

| 순서 | 작업 |
|---|---|
| 1 | `.gitignore` 작성 (`__pycache__/`, `.env`, `*.html` 등 결과 산출물 제외) |
| 2 | README.md 최종 다듬기 (Phase 1 반영: 설정 파일 사용법, 로깅 등) |
| 3 | `git init` → `git add .` → 첫 커밋 |
| 4 | GitHub에 저장소 생성 (`sitrep-fusion-agent` 등 이름으로) |
| 5 | `git push`로 업로드 |
| 6 | (선택) LICENSE 파일 추가 |

**예상 소요**: 준비만 되면 실습 자체는 짧음 (1시간 내외). Phase 1이 먼저 끝나 있어야 "정리된 상태"로 첫 커밋을 올릴 수 있어서 순서상 뒤에 배치.

---

## Phase 3 — 상태 지속성 구조 설계 (궤적 예측의 전제조건, 난이도 중~상)

지금 구조는 "매번 새로 시작하는 파이프라인"이라, 같은 항공기를 여러 번의 폴링에 걸쳐 추적하지 못합니다. 이걸 바꾸는 게 이 Phase의 목표입니다.

| 작업 | 내용 |
|---|---|
| 트랙 이력 저장소 설계 | `track_id`별로 (시간, 위치) 이력을 누적 저장하는 구조 추가 (파일 기반 JSON도 가능, 나중에 DB로 확장) |
| `continuous_monitor.py` 개조 | 매 사이클마다 새 상태로 시작하지 않고, 이전 사이클의 트랙 이력과 매칭 |
| 이력 기반 속도/방향 재계산 | 여러 시점의 위치로 실제 이동 벡터를 계산 (지금은 API가 준 순간 속도값만 그대로 씀) |

**주의**: 이 Phase가 전체 스케줄에서 가장 설계 난이도가 높습니다. 시간이 부족하면 Phase 2(GitHub 업로드)까지만 하고 여기서부터는 별도 세션으로 나눠 진행하는 것을 추천합니다.

---

## Phase 4 — 경로 예측 기능

Phase 3(상태 지속성)이 끝나야 시작 가능합니다.

| 작업 | 내용 |
|---|---|
| 간단한 선형 외삽부터 시작 | 최근 몇 개 위치로 속도 벡터 계산 → N분 뒤 예상 위치를 직선으로 추정 (칼만 필터 등 정교한 기법은 그다음 단계) |
| 지도에 예상 경로 표시 | `visualize_map.py`에 점선/화살표로 예측 경로 추가 |
| 위협 스코어링에 반영 | "예상 경로가 특정 시설/구역에 근접"하는 경우 가중치 추가 |

---

## Phase 5 — C. 배포 (FastAPI + 관제 대시보드)

**용어 정리(2026-08-06)**: "웹 앱"이 아니라 **"대시보드"**(또는 방산 전문용어로 **COP, Common Operating Picture / 공통상황도**)가 정확한 표현. 화면 전환이 거의 없이 한 화면(지도 + 근거 패널)에서 실시간 감시·조작하는 형태이고, 모바일이 아니라 상황실 고정 화면 사용을 전제로 함.

```
시스템 (전체) └─ 대시보드/COP (Phase 5에서 만들 것) └─ 에이전트(agent/graph.py의 자율 판단 로직)
```

**레이아웃 컨셉**: 지도(레이더 화면처럼)가 메인, 옆 또는 아래에 선택된 트랙의 점수 근거 패널이 연동. 지도 마커 클릭 → 패널이 해당 트랙 정보로 갱신 → 근거 리스트 표시 → 승인/거부 버튼.

| 작업 | 내용 |
|---|---|
| FastAPI 엔드포인트 작성 | `/status`, `/tracks`, `/approve` 같은 API 라우트로 파이프라인 감싸기 |
| 대시보드에서 승인/거부 버튼 | 지금 터미널 `input()`으로 하던 분석관 승인을, 대시보드 버튼 클릭으로 대체 |
| 지도-패널 연동 실시간화 | 정적 `threat_map.html` + 별도 콘솔 대신, 지도 클릭 시 근거 패널이 갱신되는 하나의 화면으로 통합 |
| **감사 로그 버그 수정** | 08/06 코드 리뷰에서 발견: `interrupt()` 이후에 감사 로그를 남기는 구조라, 분석관이 판단 없이 종료하면 로그가 안 남음. `interrupt` 호출 "전"에 대기 시작 로그를 먼저 남기도록 수정 |
| (선택) 클라우드 배포 | AWS/Render 등에 올려서 실제 URL로 접속 가능하게 |

**기존 부품과의 매핑**:

| 오늘까지 만든 것 | 대시보드에서의 역할 |
|---|---|
| `visualize_map.py` | 메인 지도 패널 |
| `threat_scoring.py`의 근거 리스트 | 근거 breakdown 패널 |
| `agent/graph.py`의 `interrupt` | 승인/거부 버튼 클릭이 대체 |
| `audit_log.py` | 버튼 클릭 시 백그라운드에서 자동 기록 |

**포트폴리오 임팩트**: 이 Phase가 완료되면 "터미널에서 실행하는 스크립트"에서 "관제 대시보드(COP)"로 격이 달라집니다. 면접에서 화면 공유로 직접 보여줄 수 있는 수준.

---

## Phase 6 — 컴퓨터비전 확장: EO/IR 표적탐지 모듈 (12월까지 목표)

**배경**: 한화에어로스페이스 AI Research(석·박사 특별채용)의 "컴퓨터비전(표적탐지)" 트랙과 직접 매칭시키기 위한 확장. 완전히 새 프로젝트로 분리하지 않고, 기존 다중 센서 위협 융합 시스템의 확장 모듈로 통합.

| 작업 | 내용 |
|---|---|
| 데이터셋 준비 | DOTA(항공 이미지 객체탐지 벤치마크) 또는 xView(60개 객체 범주, 100만+ 주석) 활용 |
| 모델 선정·파인튜닝 | Ultralytics YOLO26(2026.01 출시, OBB 방향성 탐지 지원 — 항공/위성 영상에 특히 적합)을 사전학습 모델로 파인튜닝 |
| 기존 시스템과 통합 | `synthetic_sensors.py`의 "합성 열화상" 무작위 생성 로직을, 실제 이미지 기반 탐지 결과를 반환하는 모듈로 교체(또는 병행) |
| 탐지 결과를 위협 스코어링에 연결 | 탐지된 객체 종류(차량/항공기/선박 등)를 `threat_scoring.py` 근거에 반영 |
| (선택) 웹 데모에 반영 | Phase 5의 웹 서비스에 탐지 결과 시각화(바운딩 박스) 추가 |

**목표**: "다중 센서 위협 융합 에이전트"라는 하나의 서사 안에, 실제 이미지 기반 인지(컴퓨터비전)까지 포함된 완결된 시스템으로 확장.

---

## Phase 7 (선택, 시간 남을 경우) — 자율주행 확장

Phase 4(경로 예측)가 이미 궤적 예측을 다루므로, 시간이 남으면 이를 실제 경로 계획/회피 알고리즘 수준으로 심화하는 방향. 완전 신규 프로젝트가 아니라 Phase 4의 연장선.

---

## 전체 순서 요약

```
Phase 1 (작은 보완) → Phase 2 (GitHub 업로드)
                          ↓
                    Phase 3 (상태 지속성) → Phase 4 (경로 예측)
                          ↓
                    Phase 5 (FastAPI 배포)
                          ↓
                    Phase 6 (컴퓨터비전 확장 — 12월까지 목표)
                          ↓
                    Phase 7 (선택, 자율주행 확장 — 시간 남으면)
```

Phase 3~5는 Phase 2 이후라면 순서를 바꿔도 무방합니다 (예: 배포부터 하고 싶으면 Phase 5를 Phase 3보다 먼저 해도 로직상 문제없음). 다만 **"경로 예측"은 반드시 "상태 지속성" 이후**여야 합니다. Phase 6은 독립적이라 Phase 2 완료 후 언제든 시작 가능하지만, 하나의 통합 서사를 위해 Phase 5(웹 서비스) 이후 진행을 권장.

## 시간이 부족할 경우 우선순위

12월까지 시간을 고려한 최종 우선순위입니다:
1. Phase 1 + Phase 2 (GitHub 업로드까지)는 반드시
2. Phase 5 (배포) — 경로 예측보다 시연 임팩트가 큼
3. **Phase 6 (컴퓨터비전 확장)** — 취업 전략상 우선순위 높음, 12월까지 목표로 확보
4. Phase 3~4 (상태 지속성·경로 예측) — 어렵고 시간이 많이 드는 작업, 여유 있을 때
5. Phase 7 (자율주행 확장) — 정말 시간이 남을 때만

---

## 8주 간트 일정 (가정: 마감일·주당 투입시간 미지정, 순차 진행 기준)

| 과제 | 1주 | 2주 | 3주 | 4주 | 5주 | 6주 | 7주 | 8주 |
|---|:---:|:---:|:---:|:---:|:---:|:---:|:---:|:---:|
| Phase1 작은 보완 | ■ | | | | | | | |
| Phase2 GitHub 업로드 | | ■ | | | | | | |
| Phase3 상태 지속성 설계 | | | ■ | ■ | | | | |
| Phase4 경로 예측 기능 | | | | | ■ | ■ | | |
| Phase5 FastAPI 배포 | | | | | | | ■ | |
| 포트폴리오 마무리 | | | | | | | | ■ |

**실제 마감일이나 주당 투입 가능 시간이 정해지면 이 표를 그에 맞게 다시 배치 가능** (현재는 순차 진행 가정만 반영).

## Phase별 작업내용 → 결과 제출물

| Phase | 작업 내용 | 결과 제출물 |
|---|---|---|
| 1. 작은 보완 | .env 자동로드, config 분리, logging 전환, OpenSky 계정 등록 | 정리된 코드베이스 |
| 2. GitHub 업로드 | .gitignore, README 다듬기, 커밋, push | 공개 저장소 URL |
| 3. 상태 지속성 설계 | 트랙 이력 저장 구조, 폴링 간 매칭 로직 | 이력 기반 상태 관리 모듈 |
| 4. 경로 예측 | 선형 외삽, 지도에 예상 경로 표시 | 궤적 예측 기능 데모 |
| 5. FastAPI 배포 | API 엔드포인트, 웹 승인 버튼, 실시간 대시보드 | 접속 가능한 웹 서비스 URL |
| 포트폴리오 마무리 | 발표자료, 시연 영상 제작 | 최종 포트폴리오 패키지 |

---

## 외부 일정과의 조건부 병행 (2026-08-06 기준, 08-06 업데이트 반영)

- **SQLD 자격증 시험**: 8월 20일경(2주 후). DB 수업 3~4주 수강한 상태라 하루 1시간 학습으로 충분 — 국방 프로젝트에 큰 영향 없음, 병행 진행.
- **소방안전 공모전**: 기획서(1차) 제출 완료, **결과는 8/17 이전 확인 예정**. 조건부 분기:
  - **1차 통과 시**: 8/17~9/9 소방안전 2차(발표자료+시연 HTML) 집중 → 이 기간 국방 프로젝트 일시정지
  - **1차 탈락 시**: 국방 프로젝트 중단 없이 계속 진행
  - 8/17 시점에 결과 확인 후 실제로 어느 쪽인지 이 파일에 업데이트할 것
  - 참고: 시연 HTML 제작 시 `visualize_map.py`(folium 인터랙티브 지도) 경험이 재활용 가능할 수 있음
- **스마트인재개발원 AI 에이전트 풀스택 엔지니어 과정(1기, 광주 동구, ~12/16 종료)**:
  - 총 프로젝트 3개 중 1차(미니프로젝트) 완료
  - **2차 프로젝트: 8월 20일경 시작, 5인 팀 프로젝트, 정확한 기간·주제는 미정** (진행되면서 업데이트 필요)
  - **우선순위 최상위로 취급** — 팀 작업이라 본인 페이스로 조절 불가하므로, 2차 시작되면 국방 프로젝트는 자동으로 후순위
  - 3차 프로젝트 시기도 추후 확인되는 대로 반영

### 8/17~20 구간 — 세 일정이 몰리는 최대 혼잡 구간

```
SQLD 시험(~8/20)          ─ 병행 가능 (하루 1시간)
2차 팀 프로젝트(8/20~, 기간 미정) ─ 시작되면 최우선
소방안전 2차(통과 시 8/17~9/9)   ─ 조건부, 통과 시 2차 팀프로젝트와 겹칠 가능성 높음
```

**이 구간 동안 국방 프로젝트는 사실상 정지 상태로 보는 게 현실적.** 정확한 재개 시점은 2차 팀 프로젝트 기간이 확인되는 대로 갱신.
